matrix:
  include:
    - language: node_js
      node_js:
        - 7
      cache:
        directories:
          - node_modules
      before_script:
        - cd webapp
      script:
        - npm run build
        - npm test
